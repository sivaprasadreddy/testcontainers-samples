name: Build spring-boot-jpa-demo

on:
  push:
    paths:
      - spring-boot-jpa-demo/**
    branches:
      - '**'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./spring-boot-jpa-demo
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Test
        run: ./mvnw verify
